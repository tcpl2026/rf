# a project example

```
mkdir variables keywords lib testcases
//variables放变量
//keywords放关键字
//lib放自定义python库
//testcases放置测试用例

--------------------------------------

vim lib/CustomerLib.py

import os
import time

class CustomerLib:
    def get_screenshots_path(self):
        root_dir = os.path.dirname(os.path.dirname(__file__))
        screenshots = os.path.join(root_dir, "screenshots")
        if not os.path.exists(screenshots):
            os.mkdir(screenshots)

        y_m = time.strftime("%Y_%m")
        ym_dir = os.path.join(screenshots, y_m)
        if not os.path.exists(ym_dir):
            os.mkdir(ym_dir)

        return ym_dir

if __name__ == '__main__':
    cm = CustomerLib()
    dir = cm.get_screenshots_path()
    print(dir)

---------------------------------------

vim keywords/useraction.txt

*** Settings ***
Library    SeleniumLibrary
Library    ../lib/CustomerLib.py

*** Keywords ***
take a screenshot
    ${pngdir}=    Get Screenshots Path
    Set Screenshot Directory    path=${pngdir}
    Capture Page Screenshot

----------------------------------------

vim testcases/test.txt

*** Settings ***
Library     SeleniumLibrary
Resource    ../keywords/useraction.txt

*** Variables ***
${url}        https://www.baidu.com/
${browser}    Chrome

*** Test Cases ***
access baidu
    Open Browser    url=${url}    browser=${browser}
    take a screenshot
    Input Text    xpath://*[@id="kw"]    robotframework

```
